52 Frete Clientes A = 
SUMX(
    FILTER(                                         -- Filter Retorna uma Tabela
        fVendas,                                    -- Retornando Tabela fvendas
        RELATED(dCliente[Classificacao]) = "A"      -- Filtrando de acordo com a Tabela dClientes
    )
)
