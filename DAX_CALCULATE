20 America do Norte Vendas =
CALCULATE(
    [05 Total Gross Sales],
    dLocal[Continente] = "America do Norte"
)

-- Calculate funciona como a WHERE no SQL


CALCULATE(
    [05 Total Gross Sales],
    ALL(dVendedor)
)

-- O ALL tira todos os filtros, nesse caso esta tirando da tabela inteira.
